# =============================================================================
# Makan72 — Ficheiros que o git ignora
# =============================================================================

# Cache e temporários (tudo descartável)
08-logs/cache/
08-logs/logs/*.log

# Sinais em tempo real (temporários)
04-bus/heartbeat/
04-bus/status/
04-bus/alerts/

# Segredos
01-config/.env
01-config/secrets.*
*.env

# Sistema
__pycache__/
*.pyc
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Checkpoints (opcionais — CEO decide se quer versionar)
08-logs/cache/checkpoints/

# Backups locais (já estão no git como histórico)
07-archive/backups/

# Logs de execução (reconstrutíveis)
*.log

# =============================================================================
# Adicionado Fase 2 Parte 5A (2026-03-08)
# =============================================================================

# Contexto temporario de agentes (regenerado a cada sessao)
09-workspace/context/

# Historico de sessoes (local)
07-archive/SESSAO_*.md

# Inbox de agentes (criado dinamicamente por manage-agents.sh)
# Nota: NAO ignorar 03-inbox/ceo/ (estatico, deve estar no git)
03-inbox/CLAUDE/
03-inbox/QWEN/
03-inbox/GEMINI/
*.lock

# =============================================================================
# Adicionado FASE TOOLS (2026-04-04)
# =============================================================================

# Modelos AI (grandes, binários)
10-tools/models/*.safetensors
10-tools/models/*.ckpt
10-tools/models/*.bin
10-tools/models/*.pth

# ComfyUI (clone externo)
10-tools/comfyui/
00-global/.inbox_token
.inbox_token

# Ficheiros gerados em runtime (não devem ser commitados)
01-config/layouts/makan72.kdl
04-bus/active_slots.json

# Dados do utilizador (cada instalação tem os seus)
01-config/projects.json
